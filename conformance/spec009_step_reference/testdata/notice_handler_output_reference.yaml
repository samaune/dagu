working_dir: .
steps:
  - id: build
    run: |
      printf 'artifact=from-handler\n' >> "$DAGU_OUTPUT_FILE"
    outputs:
      - name: artifact

handler_on:
  success:
    run: |
      printf '%s\n' '${steps.build.outputs.artifact}' > handler-output-reference.txt
