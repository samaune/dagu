working_dir: .
steps:
  - id: build
    run: |
      printf 'image_tag=v1.2.3\n' >> "$DAGU_OUTPUT_FILE"
    outputs:
      - name: image_tag

  - id: deploy
    depends: build
    run: |
      printf '%s\n' '${steps.build.outputs.image_tag}' > resolved-string.txt
