shell: cmd
working_dir: .
params:
  - name: environment
    type: string
    default: prod
  - name: service_name
    type: string
    eval: $(Write-Output '${params.environment}-api')
steps:
  - name: write
    run: echo(${params.service_name}>windows-reference-order.txt
