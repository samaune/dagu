params:
  - name: environment
    description: Runtime environment.
working_dir: .
handler_on:
  success:
    run:
      - "printf '%s\\n' '${params.environment}' > handler-run-array.txt"
steps:
  - name: ok
    run: "true"
