params:
  - name: environment
    description: Runtime environment.
working_dir: .
handler_on:
  success:
    run: |
      printf '%s\n' prod >&2
    stderr: "handler-stderr-${params.environment}.txt"
steps:
  - name: ok
    run: "true"
