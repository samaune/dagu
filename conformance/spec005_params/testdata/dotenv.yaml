params:
  - name: environment
    description: Runtime environment.
working_dir: .
dotenv:
  - ".env.${params.environment}"
steps:
  - name: write
    run: |
      printf '%s\n' "$DOTENV_VALUE" > dotenv.txt
